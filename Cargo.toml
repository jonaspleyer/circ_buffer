[package]
name = "circ_buffer"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1.0.204", optional = true }

[dev-dependencies]
serde = { version = "1.0.204", features = [ "derive" ] }
serde_json = "1.0.120"

[features]
serde = [ "dep:serde" ]
